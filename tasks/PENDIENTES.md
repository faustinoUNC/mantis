# Pendientes — MANTIS 2

> Única fuente de verdad de los pendientes del proyecto. Al completar uno,
> moverlo a "Hechos" con la fecha. Última actualización: 2026-07-19.

## Ahora (esta semana)

- [ ] **Fotos del cliente en el inbox (STORY-1021) — IMPLEMENTADA y verificada E2E, sin commitear** (2026-07-21) — pedido de Fausti: si el inquilino/propietario mandaba el reporte por mail con foto, la foto se perdía en silencio (la ingesta solo extraía el texto). Ahora la sincronización baja los adjuntos de imagen (hasta 5, ≤10 MB) al bucket `gestiones` (`inbox/<gmail_message_id>/…`), la card del inbox muestra las miniaturas y al convertir en gestión los paths viajan a `gestiones.fotos_reporte_paths` — el detalle muestra "Fotos del reporte" bajo la descripción para todos los roles (el técnico ve el problema antes de ir). Migración `story_1021_adjuntos_inbox` aplicada. E2E con mails reales (1 foto y 3 fotos, vista gestor + técnico). Ojo: hasta el deploy, el cron de Vercel (código viejo) ingiere los mails nuevos sin fotos.
- [ ] **Tope de adelanto de materiales (STORY-1018, card #140) — IMPLEMENTADA y verificada E2E, pusheada** (2026-07-20) — resolución del party (15ª sesión, con Fausti): aviso ámbar con confirmación en el paso de confirmar existente del adelanto cuando el acumulado supera `materiales del presupuesto vigente + ampliaciones aprobadas del técnico actual` (ampliación suma entera a sabiendas; mano de obra afuera). NUNCA bloqueo (la v1.1 de STORY-977 removió el tope duro por romper casos legítimos — esta es la reintroducción consciente en forma de aviso). `excedente_tope` congelado en el evento `adelanto_materiales_registrado` para Auditoría. El "límite que sigue a la re-presupuestación" no se programa: la fórmula lee el presupuesto vigente (verificado en el E2E del 2026-07-20).
- [ ] **Ciclo de vida de los adelantos (STORY-1019, card #141) — IMPLEMENTADA y verificada E2E** (2026-07-20) — resolución del party (15ª sesión, con Fausti; reencuadre final de Fausti: registrar TODOS los adelantos, no solo los congelados). Pestaña "Adelantos" en Finanzas (En obra / A resolver / Saldados por mes) sobre derivación ÚNICA en `features/finanzas/` — cero tablas: la liquidación salda sola el camino feliz; "a resolver" = desasignación (neto de devolución), cancelación con adelanto y sobrante de liquidación. Evento nuevo `adelanto_saldado` (nota obligatoria, botón en la constancia, solo admin+administrativo, sin fila en matriz). Card en perfil staff del técnico + aviso ámbar al liquidar con deuda abierta (no bloquea ni descuenta). Walter: la tool de finanzas envuelve la misma lectura (caso real: "¿qué técnicos desasigné con adelanto dado?" hoy no lo puede responder). Revierte conscientemente "sin lista global / sin marcar resuelto" de STORY-1014.
- [ ] **Ampliación de presupuesto durante la obra (STORY-1017, card #123)** (2026-07-20) — resolución del party (14ª sesión, con Fausti): la re-aprobación post-obra que proponía la card era teatro (con `costo_final` de fórmula cerrada, un "no" después de gastar no cambia nada) → el permiso se pide ANTES de gastar. El técnico pide el extra en plena ejecución (monto + motivo, tabla nueva `ampliaciones` — NO filas en `presupuestos`: 10+ consumidores asumen un solo aprobado); el gestor la manda por email al pagador (sin PDF, los tres números en el cuerpo) y recién ahí puede registrar la autorización (gate `enviada_pagador_en`, espejo STORY-935); rechazo con motivo notificado al técnico; desasignar rechaza las del saliente (`avanzar_etapa()`); en Cobro, caja ámbar (aviso, NO candado) si el costo final supera lo autorizado. `codigo/features/gestiones/{types,service,eventos}.ts`, `codigo/features/finanzas/service.ts`, `codigo/components/gestiones/{detalle,finanzas}.client.tsx`, `scripts/avanzar_etapa.sql`, 3 migraciones — `tsc`/eslint verdes, **E2E navegador OK** (circuito completo aprobar + rechazar en gestión #106; ámbar en #95: $1.016.000 vs $850.000). Commit `628e7f3`, card #123 en "En prueba".
- [ ] **Walter borraba el chat al cambiar de sección (STORY-1015, card #135)** (2026-07-19) — reportado por Rami: Walter perdía la conversación al moverse de un apartado a otro. Causa: `PanelShell` (que monta a `<Walter>`) vive en un layout por cada sección de la app (11 layouts hermanos del App Router); al navegar entre secciones el layout se re-monta y `useChat`, que guarda los mensajes solo en estado local, arranca vacío. La posición de la burbuja ya sobrevivía porque se persiste en `sessionStorage`; el chat no. Fix: persistir `messages` en `sessionStorage` (clave `walter-chat`, por pestaña) restaurándolos en el `rAF` de montaje y guardándolos en cada cambio (ref `restaurado` para no pisar lo guardado con el `[]` inicial); "Nueva" limpia la clave. Sin tablas ni backend (Regla #0). `codigo/components/asistente/walter.client.tsx` — `tsc`/eslint verdes, **E2E navegador OK** (chatear → navegar Tablero→Finanzas → reabrir intacto; "Nueva" + navegar → saludo inicial).
- [ ] **Tarjeta del tablero rota cuando el técnico no continúa (STORY-1016, card #137)** (2026-07-19) — reportado por Fausti con captura: en una columna del tablero el badge "Técnico no continúa" desbordaba, se partía en dos renglones inflando una caja ámbar enorme y aplastaba la especialidad a "G…". Causa: el badge (STORY-976) no tenía `shrink-0` ni `whitespace-nowrap`, así que en la columna angosta se encogía hasta el ancho de su palabra más larga y el texto se envolvía. Fix: badge corto **"En pausa"** (mismo nombre que ya usa la vista del técnico para ese estado) en una sola línea (`shrink-0 whitespace-nowrap`); la especialidad recupera su espacio. Solo el tablero — el detalle conserva "Técnico no continúa" (ahí hay ancho de sobra). `codigo/components/gestiones/tablero.client.tsx` — `tsc`/eslint verdes, **E2E navegador OK** (gestión #188 en Presupuesto: "Gas" + badge "En pausa" en una línea, sin romperse).
- [ ] **Al agregar una especialidad nueva que exige matrícula, ahora sí obliga a cargarla (STORY-1012)** (2026-07-19) — reportado por Giuliano: al agregarle a un técnico ya creado una categoría con matrícula obligatoria (ej. "Gasista"), si el técnico ya tenía matrícula de OTRA especialidad el sistema dejaba pasar sin pedir la nueva. Causa: STORY-948 (2026-07-18) había hecho el campo "obligatorio solo si el técnico no tiene ninguna matrícula todavía" — miraba si tenía CUALQUIER matrícula, no si tenía la de la especialidad recién agregada. Fix: el bloqueo ahora compara contra las especialidades que el técnico ya tenía ANTES de este submit (`tecnico_especialidades` previas); si hay una exigente nueva, pide subir un archivo en el mismo submit sin importar cuántas matrículas viejas tenga de otras especialidades. Implementado en `codigo/features/tecnicos/service.ts` (`actualizarEspecialidadesTecnico`) y `codigo/components/tecnicos/especialidades-tecnico.client.tsx` — **falta correr `tsc`/`eslint` y probar E2E en navegador** (no había Node disponible en el entorno donde se hizo el fix).
- [ ] **El gestor de mantenimiento (comercial) puede cargar adelantos en ejecución (STORY-1011)** (2026-07-19) — pedido de Fausti: antes el adelanto de dinero al técnico (STORY-977) era exclusivo del administrativo/admin; ahora el gestor de mantenimiento dueño de la gestión también puede cargarlo desde `en_ejecucion` (guard nuevo `exigirAdelanto`, ownership vía `gestor_id`; cobro/liquidación siguen siendo solo del administrativo). El UPDATE pasa a admin client porque el trigger de Postgres `proteger_gestiones_update` no está versionado en el repo y no hay forma de confirmar desde acá si ya contempla a `gestor_mantenimiento`. Implementado en `codigo/features/finanzas/service.ts` y `codigo/components/gestiones/detalle.client.tsx` — **falta correr `tsc`/`eslint` y probar E2E en navegador** (no había Node disponible en el entorno donde se hizo el fix; en particular hay que confirmar contra la DB real que el gestor de mantenimiento efectivamente puede cargar el adelanto).
- [x] **Número identificador visible de gestión (STORY-1009, card #128)** (2026-07-19) — dos gestiones parecidas en la misma casa eran indistinguibles; el comentario de Fausti en la card fijó la solución: un ID visible de afuera y de adentro del detalle. Columna `numero` secuencial asignada por la DB (backfill cronológico de las 192 existentes), `#N` en la tarjeta del tablero, header del detalle (reemplaza el prefijo UUID `N° 3F2A8B91`), mis-trabajos del técnico y archivadas; buscable por N° (con o sin `#`) en las tres vistas; el combo "¿Surgió de otra gestión?" y los PDF de nota/presupuesto/detalle llevan el mismo número. E2E verificado en las 3 vistas + detalle.
- [x] **Nuevo logo: ícono de la pestaña + ícono al agregar la app al celular (STORY-1010)** (2026-07-19) — Fausti pidió reemplazar el favicon/ícono de la app por la nueva imagen (cabeza de mantis blanca sobre fondo verde). Solo assets: `codigo/app/icon.png` (512), `apple-icon.png` (180) y `favicon.ico` (16/32/48) regenerados desde la imagen nueva — Next.js los detecta por convención de archivo, sin tocar `layout.tsx`. `public/brand/mantis-logo-fondo-blanco.png` y la presentación de tesis (repo/deploy aparte) quedaron fuera a propósito (no referenciados / no era lo pedido). Pendiente de confirmación visual (el favicon cachea fuerte en el navegador).
- [ ] **Walter no expone el fee de la inmobiliaria al técnico (STORY-1013)** (2026-07-19) — de la auditoría de robustez multi-agente: el único hallazgo que Fausti decidió aplicar (el resto es robustez ante fallos que no aplica al contexto tesis, queda en engram). La tool compartida `detalle_gestion` de Walter devolvía `cargo_admin` (el fee) también al rol técnico, un dato que la UI le oculta (vive solo en `FinanzasAcciones`, rol administrativo) — eco de la sobre-exposición de Walter v1. Fix de un campo: `cargo_admin` condicional al rol (`...(rol !== "tecnico" && { cargo_admin })`); el resto de lo financiero (pagador/costo final/adelanto) se mantiene por paridad con la UI. `tsc`/eslint verdes, commiteado; pendiente E2E con Walter como técnico.
- [ ] **El campo Nombre aceptaba números (STORY-1008)** (2026-07-19) — reportado por Fausti: los nombres de propietarios, inquilinos y empleados podían ser números — no había ninguna validación de formato, solo de "no vacío". Fix: util nuevo `errorNombre()` (mismo molde que `errorTelefono`/`errorCuil`) que rechaza dígitos y símbolos raros, aplicado en los 6 puntos de alta/edición (propietario/inquilino vía cartera, empleados, técnicos) tanto en el server (la validación real) como en el cliente (filtro mientras se tipea + mensaje antes de enviar). Implementado en `codigo/shared/utils/nombre.ts` + 7 archivos más — **falta correr `tsc`/`eslint` y probar E2E en navegador** (no había Node disponible en el entorno donde se hizo el fix).
- [ ] **Al rechazar la conformidad no se volvía a pedir la rendición (STORY-1006)** (2026-07-19) — reportado por Fausti: la conformidad se puede rechazar por la foto O por la rendición (total de materiales / fotos de comprobantes) que se cargan junto con ella, pero al rechazar y volver a subir, el técnico solo veía el campo de la foto — la rendición no se volvía a pedir aunque haya sido la causa del rechazo. Causa: la resubida decidía pedir la rendición solo si la etapa era `en_ejecucion`, y el rechazo no mueve la etapa (queda en `conformidad`). Fix: se separó "hay que rendir de nuevo" (ahora también true si la última conformidad de ese técnico está rechazada) de "hay que avanzar la etapa" (sigue siendo solo la primera vez, para no volver a llamar `avanzar_etapa` con una transición ya hecha). Implementado en `codigo/features/gestiones/service.ts` y `codigo/components/gestiones/detalle.client.tsx` — **falta correr `tsc`/`eslint` y probar E2E en navegador** (no había Node disponible en el entorno donde se hizo el fix).
- [ ] **Re-aprobación del propietario por desvío grande del presupuesto (reunión con Andres 18/7, card #123)** — hoy la nota de cobro puede superar lo que el propietario aprobó sin re-consultarle. Diseño acordado en la reunión: umbral de desvío parametrizado; si el costo final lo supera, reciclar el circuito del presupuesto inicial (mail + registro de la autorización) antes de cobrar.
- [ ] **Tipificar motivos de rechazo/desasignación (reunión 18/7, card #124)** — motivos hoy en texto libre → lista corta de opciones + "otro", para que Informes pueda responder POR QUÉ se traban/rechazan las gestiones (la corrección que más le gustó a Fausti de la reunión).
- [ ] **Métrica de rentabilidad por especialidad (reunión 18/7, card #127)** — cuánto fee dejó cada categoría de trabajo en el período, para saber dónde reforzar. "Es totalmente medible, lo voy a implementar" (Fausti en la reunión).
- [x] **Embudo de gestión en tiempo real (STORY-1004, card #125)** (2026-07-19) — de la reunión con Andres: el "Embudo de gestiones" contaba cuántas gestiones alcanzaron cada etapa en el período (la vista anual "no sirve para nada"). Ahora es una foto **ahora**: gestiones activas por etapa actual (excluye finalizado/cancelada), la barra más alta = cuello de botella, coloreada por magnitud (igual que "Cuellos de botella"). Se sacó de la caja "En el período" a un bloque propio con badge "ahora"; "Cuellos de botella"/"Tiempo de ciclo" quedan del período. Sin tablas ni migraciones (el dato ya estaba en la etapa actual). E2E verificado (total 75 = DB, Asignación 28 = cuello en terracota).
- [x] **Renombrar gráficos de Informes: "Cumplimiento de presupuesto/plazo" → "Desvíos" (STORY-1005, card #126)** (2026-07-19) — muestran desvíos ±, el nombre confundía el signo. Solo copy: "Cumplimiento de presupuesto" → "Desvíos de presupuesto", "Cumplimiento de plazo" → "Desvío de plazo" (+ comentarios). Las cajas del detalle ya se habían corregido con STORY-1003; esto es solo los gráficos de Informes. E2E verificado.
- [x] **Adelanto de materiales: mostrar lo presupuestado + comprobante obligatorio (STORY-1002, card #122)** (2026-07-18, commit `5daceb3`) — de la reunión con Andres: la caja de adelanto en ejecución ahora muestra "El técnico presupuestó $X en materiales" (presupuesto aprobado) y exige comprobante por cada adelanto (recibo firmado o transferencia, PDF/imagen ≤8 MB, patrón STORY-986). El path va en el detalle del evento (sin migración) y la Actividad muestra "Ver comprobante" (URL firmada). E2E verificado, dato de prueba revertido.
- [x] **Bug del desvío de la rendición: daba negativo aunque el técnico gastara de más (STORY-1003, card #121)** (2026-07-18, commit `5daceb3`) — detectado en vivo en la reunión y reproducido con "Prueba Desvio": la caja de Conformidad comparaba lo rendido (SOLO materiales) contra el presupuesto TOTAL (mat + mano de obra), siguiendo un comentario falso del código → −$29.999,94 (−33%) donde correspondía +$10.000 (+20%). Fix: desvío = rendido − materiales presupuestados (como ya medía Informes/STORY-937) + labels desambiguados: el técnico ahora carga "Total final gastado en materiales ($) — sin contar tu mano de obra" y las cajas dicen "Gastado en materiales (rendido)". E2E verificado con la gestión real.
- [x] **Tipo de inmueble: Duplex + darle uso + unidad/piso opcional + dirección cortada (STORY-999)** (2026-07-18) — al select de tipos de Giuliano (`d7ab4d3`) le faltaba **Duplex** (agregado). El tipo no se usaba fuera de la cartera: ahora la card del detalle de gestión lo muestra bajo la dirección. Esa dirección salía **cortada** (doble `truncate`): ahora envuelve. Y se sumó **un único input opcional "Piso / unidad"** (nueva columna `unidad text` en `propiedades`, texto libre sin CHECK) para toda sub-descripción de ubicación (piso+depto, casa/lote en complejo, nº de local) — un solo campo para no complejizar (REGLA #0). Se muestra `tipo · unidad` en el detalle de gestión, el detalle de propiedad y el listado. Se setea en el alta (no hay edición de propiedad existente hoy — pendiente aparte si hace falta). `tsc`/`eslint` verdes, E2E navegador (alta con Duplex+unidad → detalle propiedad y detalle gestión).
- [x] **Tipo de inmueble era texto libre en el alta de administración** (2026-07-18) — el campo "Tipo" del paso "Propiedad" (alta de administración) era un input de texto libre con placeholder "Depto, Casa, Local… (opcional)", sin valores fijos. Fix: select con opciones fijas (Departamento, Casa, PH, Local, Oficina, Cochera) en vez de input libre, validado también server-side en `crearAdministracion` (rechaza cualquier valor fuera de la lista). La columna `tipo` en la tabla `propiedades` sigue siendo texto (sin migración ni CHECK constraint); el detalle y listado de propiedades no cambiaron, ya mostraban el valor guardado tal cual. `tsc`/`eslint` verdes.
- [x] **Historial de la propiedad + Resumen de obras honesto (STORY-985, party mode 2026-07-17)** (2026-07-17) — reporte de Fausti: los legajos no mostraban las obras ("no se entiende nada") y el PDF mentía ("salen siempre En curso"). Tres mentiras del PDF confirmadas: `finalizada = etapa === "finalizado"` marcaba EN CURSO todo lo que estaba en Cobro/Liquidación (trabajo terminado) y las canceladas para siempre; la "descripción" era el problema reportado, nunca el `descripcion_trabajo` del presupuesto aprobado; la fecha era la de creación, no la de terminación. Además 50/175 gestiones sin `legajo_id` eran invisibles en todo resumen. Fix: (A) PDF honesto — tres estados (Terminada con fecha real de fin de obra vía eventos / En curso / Cancelada; **canceladas sin cargo afuera, decisión de Fausti**), "qué se hizo" como texto principal + "Problema reportado"/"Reportado el" secundarios, totales del período con split inquilino/propietario; (B) sección "Historial de la propiedad" nueva en `/cartera/propiedades/[id]` — línea de tiempo con los legajos como capítulos y las obras sin legajo en capítulos "Propiedad sin ocupar", encabezado con los números del negocio (obras · terminadas · $ invertidos + quién pagó), chip ámbar de especialidad reincidente (≥3), canceladas sin cargo visibles pero apagadas y fuera de los números, cada obra linkea a su gestión; los botones del PDF se mudaron a los capítulos y "Legajos" quedó solo para acciones. Estado derivado en UN lugar (`features/cartera/historial.ts`, compartido vista+PDF): terminada = conformidad aprobada en adelante; cancelada con cargo detectada por `cargo_cancelacion` aunque esté en Cobro (lección STORY-984). Cero tablas/migraciones. E2E navegador + PDF renderizado. (2026-07-17) — reporte de Fausti confirmado con investigación BMAD y datos reales (21/117 ejecuciones computaban <1 día; 4/117 salían de ejecución por cancelación/desasignación): la métrica de plazo (card de Informes + chips del picker) usaba días fraccionales sin piso (obra de 2 horas = −98%) y contaba CUALQUIER salida de `en_ejecucion` como obra terminada (cancelada con/sin cargo y desasignación computaban como cumplimiento estelar; la desasignación se la imputaba al técnico ENTRANTE). Fix mínimo: helper nuevo `ejecucionParaPlazoDias` (solo salidas a `conformidad` — allowlist, porque la cancelación con cargo sale a `facturacion_cobro` — y piso de 1 día, coherente con el `min="1"` del form) en ambos consumidores; `ultimaEjecucionDias` intacto (el tiempo de ciclo necesita la fracción real). Case file en `_bmad-output/implementation-artifacts/investigations/`. E2E verificado en Informes y picker.
- [x] **Reasignación sin restos del técnico saliente (STORY-983)** (2026-07-17) — el técnico nuevo heredaba la inspección/avances del saliente (presupuesto habilitado sin inspeccionar, cartel "Presupuesto rechazado: Técnico desasignado", obra terminable sin avances propios). Autoría `tecnico_id` en `presupuestos`/`conformidades` (migración con backfill) y gates/carteles scoped al técnico actual. Implementada y verificada E2E en sesión previa; el commit quedó pendiente y se integró hoy.
- [x] **ComboFiltrable en los selects que crecen (STORY-981)** (2026-07-16) — tras el "quedó muy bueno" de Fausti por los combos de Auditoría, barrido completo de los desplegables de la app. Se migró lo que crece con datos reales: propiedad en Crear gestión (tablero — integra y corrige el fix `feb8805` de Giuliano, que dejaba una opción falsa "Buscar por dirección…" — e inbox), personas de cartera (`SelectorPersona`: alta de administración, legajos y cambiar propietario de una vez; ahora arranca vacío con placeholder — elegir es explícito) y gestores (filtro del tablero + reasignar). El componente ganó modo formulario: `textoTodos: null` (sin opción vacía), `placeholder` propio y `opciones` plano sin encabezados. NO se migró lo fijo y chico (urgencia, roles, medios, períodos, días, especialidad de Crear gestión) ni los pickers con patrón propio (técnico por tarjetas, especialidades multi-checkbox, etapa bottom-sheet mobile). E2E en navegador por pantalla.
- [x] **Auditoría de eventos de sistema (STORY-980, party mode 2026-07-15)** (2026-07-15) — pedido de Fausti: "hay eventos clave que no estamos logueando" (crear/aceptar/rechazar técnicos, creación de usuarios). Confirmado y peor: TODO el ABM administrativo era invisible (`eventos_gestion` exige `gestion_id` y `actor_id` NOT NULL). Solución: tabla nueva `eventos_sistema` (actor nullable — la postulación pública no tiene sesión; afectado CONGELADO en `detalle`: la evidencia sobrevive a renombres y borrados), 11 tipos de identidad y acceso (empleado creado/rol/estado/blanqueo; técnico postulado/alta manual/aprobado/rechazado con motivo/estado), helper `registrarEventoSistema()` sin "use server", y tabs "Gestiones | Sistema" en `/admin/auditoria` (`?tab=sistema`) con filtros server-side + count patrón 974 (búsqueda por afectado vía `detalle->>afectado`). Se descartó `gestion_id` nullable (trigger de notificaciones dispararía espurio, CASCADE mataría la evidencia). E2E completo: ciclo técnico entero (incl. reintento tras rechazo con actor "Registro público") + ciclo empleado, filtros validados, y los eventos siguen íntegros tras borrar a los afectados. **Afuera por decisión** (no re-proponer sin disparador real): cartera/legajos/especialidades (solo si aparece disputa real de "quién paga") y ediciones menores (nombres/especialidades/contacto — ruido, no evidencia).
- [ ] **Adelanto de materiales al técnico (STORY-977)** (2026-07-15) — Giuliano pidió revivir STORY-933 ("adelantos de obra"), descartada por Fausti el 2026-07-11 por complejidad; confirmado que ahora sí se quiere, con el diseño más simple posible: un solo campo `adelanto_materiales` (no una tabla de entregas con medio/anulación como proponía STORY-933 v1.0), cargado por el administrativo desde `en_ejecucion`, y descontado del monto a liquidar al técnico al final. Código completo (tsc/eslint verdes) y migración ya aplicada por Giuliano (columna `gestiones.adelanto_materiales numeric`) → **falta probar el flujo E2E en navegador** (carga de adelanto, header en las 3 vistas de rol, liquidación neta, PDF). **v1.1 (mismo día):** Giuliano probó y pidió 5 correcciones — (1) el campo pasa a ser aditivo (cada carga suma al total, permite más de un adelanto), (2) se elimina el tope contra el presupuesto (puede superar lo debido), (3) si el adelanto supera lo debido al técnico, la liquidación registra y muestra el "sobrante", (4) cargar un adelanto pide confirmación explícita del monto antes de guardar, (5) se saca la acción de las etapas `conformidad` y `facturacion_cobro` (queda solo en `en_ejecucion` y en `liquidacion_tecnico`). **v1.2 (mismo día):** en `liquidacion_tecnico` todavía se podía cargar un adelanto nuevo — se saca de ahí también (solo queda el desglose de lectura); el adelanto es EXCLUSIVAMENTE de `en_ejecucion`, ahora reforzado también server-side. tsc/eslint verdes — sigue faltando el E2E en navegador.
- [x] **Tercera ronda de la card #93: el aviso del técnico ahora se ve y pausa la obra (STORY-976)** (2026-07-15) — los testers rechazaron por tercera vez la card: (1) el aviso "no puedo continuar" era solo campanita + renglón en Actividad (en Presupuesto nada avisaba en la pantalla principal que el presupuesto no iba a llegar) y (2) el técnico podía seguir operando después de avisar. Causa raíz: STORY-971 lo implementó como evento suelto, sin estado en la gestión. Fix: campo explícito `aviso_no_continua_en/motivo` (patrón `desasignada_en`) → banner ámbar arriba del detalle del gestor (motivo + fecha + salidas), badge "Técnico no continúa" en tablero y encabezado, obra EN PAUSA para el técnico (UI bloqueada + guards server en presupuestar/avanzar/terminar; su lista lo muestra "En pausa"; el aviso ya no es re-enviable tras recargar), botón "El técnico continúa" (evento `aviso_resuelto` + notificación al técnico vía matriz) y TODA transición del funnel limpia el aviso (una reasignación no arrastra el banner). E2E verificado con dos usuarios.
- [x] **Mejora de la Auditoría (STORY-974, party mode 2026-07-15)** (2026-07-15) — Fausti definió el uso: control interno "quién hizo qué". (1) El filtro dejó de mentir: `historialGlobal` filtraba client-side sobre los últimos 200 eventos globales (la DB ya tenía 1.863 → el admin veía el 11% pareciendo completo); ahora persona (select con rol), tipo, dirección/descripción y rango de fechas van en la query, con paginación server-side y count total siempre visible (validado 1:1 contra SQL). (2) Datos que se guardaban y no se veían: label de `tecnico_no_continua` (aparecía el slug crudo), `imputado`/`tecnico_saliente` de las desasignaciones (el saliente se congela como UUID — `nombrarSalientes()` lo traduce en Auditoría y Actividad), detalle expandible por fila; labels y `detalleLegible` centralizados en `features/gestiones/eventos.ts` (los dos mapas duplicados ya habían divergido). (3) Separadores por día + rol junto al actor. **Descartado por Fausti: export CSV/PDF** (uso interno, "no hace falta"); tampoco realtime ni timeline visual. E2E verificado. **v1.1 (mismo día):** botón de actualizar (ícono refresh junto al contador) a pedido de Fausti — re-consulta con los filtros vigentes; lo descartado era la tabla moviéndose sola, no traer lo nuevo a demanda.
- [x] **Cards con etiqueta "Revisar" de "Por hacer" (#6 y #71)** (2026-07-15) — Card #6 → STORY-973: el cobro combinado de STORY-950 no se veía en ningún lado (`medio_cobro_2` se escribía y nadie lo leía; era la pregunta de Ramiro en la card) → Actividad y Auditoría ahora muestran `Total · Medio: <label> · 2º medio: <label> ($ monto)`, y el evento `cobro_registrado` lleva el total. Card #71 (rechazo → notificación no sale): **no reproducible en el código actual** — verificado E2E completo (técnico rechaza desde la UI: sin 404, campanita del técnico limpia, el gestor recibe "El técnico rechazó la asignación" EN VIVO y su detalle vuelve solo al picker). La prueba de Giuliano (04:17–05:00) corrió contra el build de ANTES de los pushes de STORY-968/970 (~06:00): en su prueba de las 04:31 la desasignación todavía no avisaba al técnico. Pedido re-test en la card. BONUS detectado (pendiente abajo): hydration mismatch preexistente en Auditoría por la celda de fecha.
- [x] **Errores restantes de la card #93 (STORY-970/971/972)** (2026-07-15) — cierra los 5 pendientes: (1) el chip "Rechaza" del picker contaba `asignacion_aceptada === false` sobre las gestiones del técnico, pero el rechazo pone `tecnico_id = null` en el acto → NUNCA podía incrementar; ahora cuenta eventos `asignacion_aceptada/rechazada` por `actor_id` (mismo patrón que los abandonos de STORY-966: hechos históricos desde eventos, no del estado vivo). (2+3) La desasignación anula `presupuesto_enviado_en` (migración `story_970_...` + data fix): el gate de aprobar exige el envío del presupuesto NUEVO y el botón vuelve a decir "Enviar" (no "Reenviar"). (4) STORY-971: el técnico tiene "Avisar al gestor" (motivo obligatorio, etapas post-aceptación) → evento `tecnico_no_continua` + notificación vía outbox; NO mueve el funnel — el gestor decide con desasignar/cancelar (Regla #0: cero estados nuevos). (5) STORY-972: la cancelación con cargo tiene nota de cobro (mismo circuito `EnvioDocumento`, total = cargo, PDF "Trabajo cancelado", email "Cargo por cancelación — {dirección}") — supersede la decisión #3 de STORY-967. Todo E2E verificado.
- [x] **Errores de la prueba de la card #92 (STORY-969)** (2026-07-15) — los dos reportes sobre la rendición de STORY-965: (1) "solo permite una foto" era el file input nativo pisando la selección en cada apertura del picker (elegir varias juntas andaba, pero el flujo real es una foto por ticket) → `InputArchivo` en modo `multiple` ahora ACUMULA (estado `File[]` + `DataTransfer`, lista con × para quitar, compresión por foto al agregarla); (2) "no se ve la foto del comprobante en Actividad" → el evento `materiales_rendidos` más reciente lleva la galería de miniaturas (la evidencia ya no desaparece al aprobar la conformidad). E2E verificado. Card #92 movida a "En prueba".
- [x] **Errores 3–6 de la prueba de la card #93 (STORY-968)** (2026-07-15) — el técnico ahora SE ENTERA cuando lo desasignan o le rechazan el presupuesto: notificación al saliente creada por el service (el trigger outbox no puede: cuando corre, `tecnico_id` ya es NULL) con `gestion_id`/`ruta` null (sin link → sin 404, inmune a la limpieza STORY-951); refresco en vivo de home y detalle del técnico suscribiéndose al INSERT de sus `notificaciones` (el UPDATE de `gestiones` NO llega por realtime cuando la fila sale del alcance RLS del suscriptor — limitación estructural, no bug nuestro); y gestión no visible para un técnico → redirect a `/tecnico` en vez de `notFound()`. E2E con técnico mirando en vivo. **Quedan de la card #93**: métrica de rechazos que no incrementa (¿cuál contador?), flujo/aviso de cancelación para el técnico, texto "Reenviar" → "Enviar" con técnico nuevo, gate de aprobar sin re-enviar el presupuesto nuevo, y comprobante/email del cargo de cancelación.
- [x] **Desvincular técnico a mitad del flujo + cancelación con cargo (STORY-966/967, card Trello #93)** (2026-07-14) — unifica las ex-cards #12/#13/#22 (archivadas). Implementado y verificado E2E el mismo día del party mode: primitivo único "desasignar técnico" (retroceso TOTAL a Asignación; historial/avances se conservan; abandono imputado al técnico vía evento congelado — chip "Abandonó" en el picker REEMPLAZA a "Cancela", decisión de Fausti, y aparece en el ranking de Informes), baja de técnico BLOQUEADA con gestiones operativas (error visible, no bloquea en cobro/liquidación), marca de urgencia explícita `desasignada_en` (badge "Reasignar"), cancelación con cargo opcional y libre post-aceptación (pasa por Cobro con el circuito existente y cierra en `cancelada`; el cargo queda como `cobrado_monto`/`cobrado_fee`), fix del doble conteo de tiempos en cuellos/ciclo/plazo (última visita por etapa — bug latente que ya existía con `presupuesto→asignacion`), `ETAPAS_TERMINALES` centralizado, picker con chips "Presupuesto"/"Plazo" consistentes con Informes y card "Se pasaron del plazo" renombrada a "Cumplimiento de plazo". Falta prueba manual en producción post-deploy.
- [x] **Simplificación de gastos imprevistos (STORY-965)** (2026-07-14) — pedido de Fausti, debatido en party mode: se eliminó la feature entera (tabla `gastos_imprevistos` dropeada, UI de carga/lista, validación `total ≥ Σ gastos`, evento `gasto_enviado` y su notificación). El control de gastos es la rendición única de la 964, ahora con **fotos múltiples de comprobantes** (una por ticket, `materiales_fotos_paths text[]`) y galería + desvío del lado del gestor. `tsc`+`eslint` verdes, E2E local completo (técnico rinde 2 fotos → gestor ve galería y desvío +50% → costo final server-side correcto).
- [ ] **Prueba manual en producción de las 3 cards recién pusheadas (STORY-962/963/964)** — código pusheado y E2E verificado en LOCAL (2026-07-14, commit `226e167`), pero falta confirmar en la app deployada. Cards Trello #85/#86/#88 movidas a "En prueba" con pasos de prueba en la descripción de cada una.
- [x] **3 cards de bugs (STORY-962/963/964)** (2026-07-14, commit `226e167`) — card #86 → STORY-962: legajo cerrado (`fecha_fin` ≠ null) → la gestión trata al inquilino como "ya no está", solo propietario (contacto, pagador, email/PDF). Card #85 → STORY-963: CUIL no puede ser de dos personas distintas entre propietarios e inquilinos (bloqueo cruzado inteligente: mismo nombre = misma persona = permitido). Card #88 → STORY-964: la rendición pasa a UN solo total real de la obra (imprevistos incluidos); los gastos imprevistos son evidencia con ticket, no se re-suman; desvío real. **Supersede la decisión #1 de STORY-961**. `tsc`+`eslint` verdes, E2E local completo (Playwright + Supabase MCP).
- [x] **2 cards de "Pendiente - Bugs" (STORY-960/961)** (2026-07-13, commit `b071d5f`+`727b900`) — card #81 → STORY-960: CUIL obligatorio en alta/edición de propietario e inquilino. Card #82 → STORY-961: en Conformidad el costo final se calcula (no editable), aprobar conformidad separado del cobro. (Nota: STORY-964 revierte la decisión #1 de 961 sobre cómo rinde el técnico.)
- [x] **UX menor en pago combinado (STORY-950)** (2026-07-18, commit `08d0e7b`) — Al tildar "Pago combinado" con el monto del medio 2 vacío/cero, el botón quedaba habilitado y el submit cortaba en silencio (`finanzas.client.tsx`: `monto2Num <= 0` no estaba en el `disabled` ni tenía mensaje). Reportado por Giuliano como "no se puede pagar con tarjeta de crédito si es el medio 1" — el bug es agnóstico al medio elegido, pero con tarjeta en medio 1 la atención va al campo de recargo en vez de al monto del medio 2, lo que dispara el caso más seguido. Fix: se agregó la condición al `disabled` + mensaje "Ingresá el monto del segundo medio."
- [x] **Cobro con tarjeta/combinado/adelanto como gestor financiero: `sin_permiso` (STORY-1000, card #111)** (2026-07-18) — la card #111 volvió de "En prueba" porque el fix del trigger de hoy (entry de abajo) agregó solo las columnas `liq_*`: a la whitelist de `proteger_gestiones_update` para el rol `gestor_administrativo` le seguían faltando las columnas del COBRO — `medio_cobro_2`/`cobrado_monto_2` (STORY-950), `recargo_tarjeta_pct`/`recargo_tarjeta_monto` (STORY-975) y `adelanto_materiales` (STORY-977). Con tarjeta de crédito en el medio 1, `registrarCobro` escribe el recargo → `sin_permiso` → "No se pudo registrar el cobro" y la gestión no avanza; lo mismo bloqueaba todo pago combinado y el adelanto de materiales para ese rol. "Con tarjeta en medio 2 funciona" era porque esa prueba Giuliano la hizo logueado de ADMIN (el trigger no restringe columnas a ese rol) — verificado en el event log. Fix: migración `story_1000_whitelist_finanzas_gestor_administrativo` (esta vez versionada) que recrea el trigger con la lista completa + comentario de mantenimiento; `cargo_cancelacion` afuera a propósito (lo escribe el flujo de cancelación, no este rol). Verificado por simulación SQL del rol (las 5 columnas pasan; `etapa`/`descripcion` siguen bloqueadas) + E2E como admin (tarjeta simple y combinado avanzan a Liquidación). Tercera vez del mismo patrón: si una story suma columnas de finanzas a `gestiones`, hay que actualizar el trigger.
- [x] **Gestor financiero no podía liquidar con ningún medio de pago** (2026-07-18) — reportado junto con el bug de arriba. Causa: el trigger `proteger_gestiones_update` (Postgres) tiene una lista blanca de columnas que el rol `gestor_administrativo` puede tocar en `gestiones`; a esa lista le faltaban `liq_medio` y `liq_comprobante_path` (agregadas por STORY-946/STORY-986 sin actualizar el trigger — mismo patrón de bug ya visto una vez en STORY-914 con las columnas de cobro). `registrarLiquidacion` siempre escribe esas dos columnas, así que CUALQUIER liquidación de ese rol disparaba `sin_permiso`, sin relación con el medio de pago. Fix aplicado directo en Supabase (`CREATE OR REPLACE FUNCTION public.proteger_gestiones_update` con el array `v_finanzas` completo) — sin cambios en `codigo/` ni migración versionada en el repo. Verificado por Giuliano en la app.
- [x] **Matrículas de técnico: la administración no podía sumar una nueva ni borrar una vieja** (2026-07-18) — Giuliano recordó una restricción de una sesión anterior: al sumarle a un técnico una especialidad nueva que exige matrícula, si ya tenía al menos una matrícula cargada el formulario no dejaba adjuntar la nueva (el input solo aparecía cuando el técnico no tenía NINGUNA), y no existía ninguna forma de borrar una matrícula vieja o cargada por error. Investigado y descartado que fuera un bug de permisos por rol (administrador y gestor de mantenimiento están tratados igual en todo el feature). Fix: el input de matrícula ahora aparece siempre que la especialidad elegida la exige (obligatorio solo si el técnico no tiene ninguna todavía, opcional si ya tiene y quiere sumar otra); nueva acción `eliminarMatriculaTecnico` con botón de borrar (ícono papelera + confirmación) en la card "Documentación" del técnico, bloqueada si es la última matrícula y el técnico todavía tiene una especialidad que la exige. El DNI no se puede borrar desde ahí. `tsc`/`eslint` verdes.
- [x] **Hydration mismatch en Auditoría (fecha)** (2026-07-15, patch v1.1 de STORY-973) — `toLocaleString("es-AR")` renderizaba la hora con espacios unicode distintos en server y cliente → error de hidratación en consola y tabla regenerada client-side. Fix: `fechaHora` determinístico propio en `auditoria.client.tsx` (mismo patrón y motivo documentado que el del detalle), formato `dd/mm/aa, hh:mm` 24 h. Verificado: consola limpia.
- [ ] **Activar leaked password protection** — Dashboard de Supabase → Authentication → Sign In / Providers → toggle "Leaked password protection". 2 minutos.
- [ ] **Prueba manual del flujo de presupuesto** — En la app deployada, como gestor: elegir pagador → vista previa → enviar presupuesto → aprobar. Confirmar que el email va al pagador elegido y que el total de la nota coincide con lo aprobado (fixes de STORY-906). 5 minutos.

## Salida a producción

- [ ] **Verificar dominio en Resend** — Dashboard de Resend → Domains → agregar dominio → cargar registros DNS (SPF/DKIM) → al quedar "Verified", cambiar el remitente `onboarding@resend.dev` por uno del dominio en el código de email. Hasta entonces los emails solo llegan a la casilla de prueba.
- [ ] **Relajar el filtro del inbox** — Cuando haya casilla de mail exclusiva de mantenimiento, cambiar la query `subject:mantenimiento` en `codigo/features/inbox/sync.ts` para ingerir todo lo que llegue (hoy es necesario porque la casilla es compartida).

## Descartados (no hacer)

- ~~Export CSV/PDF de la Auditoría, realtime en esa pantalla y timeline visual con iconos~~ — Descartados por Fausti (2026-07-15, party mode de STORY-974): la Auditoría es de uso interno ("quién hizo qué"), nadie externo la ve — "no hace falta eso del csv y pdf". La tabla con separadores por día alcanza. No re-proponer.
- ~~Métrica "Rechazos por tipo" en Informes~~ — ELIMINADA por Fausti (2026-07-13): "no es valiosa para el negocio". Nació en STORY-914, se arregló en STORY-919 y se quitó en STORY-919 v1.1 (el desglose presupuesto/conformidad/asignación no cambia ninguna decisión). No re-proponerla.

- ~~Adelantos de obra al técnico (STORY-933)~~ — Descartada por Fausti (2026-07-11) el mismo día de aprobarse, antes de implementar: "no nos complicamos" con plata adelantada. En su lugar: rendición de comprobantes + total gastado al terminar la ejecución, y liquidación = materiales rendidos + mano de obra (STORY-934).
- ~~CRON_SECRET del job pg_cron a Vault~~ — Decisión de Fausti (2026-07-06): riesgo bajo, no se hará.
- ~~Métrica "qué técnico es más barato" (comparar mano de obra entre técnicos)~~ — Descartada (2026-07-08, 2ª party mode, STORY-915). No se puede hacer confiable: el único normalizador de alcance (`presupuestos.plazo_dias`) es auto-reportado y poco fiable, y un catálogo de rubros estandarizados violaría la Regla #0. Fausti: no meter datos inexactos. **En su lugar** va el **desvío de presupuesto** (cada técnico vs. su propio presupuesto), que sí es confiable.

## Hechos

- [x] **Dos cards de "Pendiente - Desarrollo": verificación de email + gestión de empleados** (2026-07-13, STORY-955/956) — (1) card #78: el registro de técnico ya no pide contraseña; la solicitud llega al staff recién cuando el técnico verifica su email (link por Resend, token propio, trigger de notificación movido al UPDATE); al aprobar le llega un email con link para crear su contraseña (`/crear-contrasena`, recovery con `hashed_token` + verifyOtp) y hay "¿Olvidaste tu contraseña?" en el login para todos los roles (`/recuperar-contrasena`, anti-enumeración + rate limit). Los alias `ausitesis+nombre@gmail.com` funcionan porque todo sale por Resend (hack `destinoEntregable`). (2) card #80: el admin edita el email de los empleados (sync auth+usuarios con rollback) y les blanquea la contraseña enviándoles el link de restablecimiento. Migración `story_955_verificacion_email` aplicada. Todo verificado E2E.
- [x] **Tres cards de "Pendiente - Bugs" + hotfix de main** (2026-07-12, STORY-951/952 + patches v1.1 de 941 y 950) — (1) card #58: quedaba "Total a facturar al pagador" en el cuadro de Acción de Cobro → "Total a cobrar" (STORY-941 v1.1); (2) card #71: 404 al abrir la notificación de una gestión rechazada — al rechazar, `responderAsignacion` ahora borra las notificaciones del técnico sobre esa gestión (STORY-951); (3) card #74: el buscador de direcciones (Photon/OSM, no Google) rankeaba por cercanía y ponía primero calles que no coincidían con lo tipeado → re-rank client-side por coincidencia de calle (STORY-952). BONUS: main estaba ROTO por la STORY-950 de Giuliano (constantes exportadas desde un archivo "use server" → 500 en el detalle de gestión) — movidas a `features/finanzas/medios.ts` (STORY-950 v1.1). Todo verificado E2E.
- [x] **Alta de administración sin inquilino + legajos solo para inquilinos libres** (2026-07-12, STORY-949, cards Trello #77 y #76) — el wizard creaba propietario+propiedad+inquilino+legajo en inserts secuenciales sin transacción: si un dato del inquilino fallaba en el server (ej. email/CUIL duplicado, invisible para la validación del cliente), la administración quedaba creada a medias y el reintento chocaba con "ya hay un propietario con ese email". Fix simple: el wizard pasa a 3 pasos (Propietario → Propiedad → Confirmar) y crea SOLO propietario+propiedad; el legajo del inquilino se abre después desde el detalle (a donde ya redirige). Además el desplegable de "abrir legajo" solo lista inquilinos activos sin legajo vigente (`listarInquilinosSinLegajo()`) + chequeo server-side en `abrirLegajo` para pestañas viejas. Sin migración. Verificada E2E.
- [x] **Registro de técnico: errores visibles en el celular + matrículas múltiples** (2026-07-12, STORY-945, commit 3ea544e) — el bug de "no sale 'Elegí al menos una especialidad' en el celu" era un 413 silencioso: las fotos de cámara (>1MB) mataban el server action antes de validar. Fix: validación client-side, try/catch con mensaje visible, compresión de imágenes en el navegador (cubre también avances/rendición/gastos/conformidad), `bodySizeLimit` 8mb, validación server de MIME/tamaño con errores claros, y matrículas múltiples (`doc_matricula_paths`, paths `matricula-N` que no se pisan). Migración en 2 fases COMPLETA: fase 1 `story_944_matriculas_fase1` + fase 2 `story_945_matriculas_fase2_drop` aplicada post-deploy (columna vieja eliminada, prod verificada).
- [x] **Cobro + Administración unificada, documentos sin comisión, presupuestación sin Causa** (2026-07-12, STORY-941/942/943, commit bb70bd6) — etapa "Cobro"; sección "Administración"/"Administraciones" sin ABMs sueltos (alta/edición/cambio de propietario e inquilino desde la propiedad); PDFs al pagador solo con el total (fee adentro) y resumen con lo cobrado; fuera "Causa" y `pagador_sugerido` (pagador explícito en Presupuesto, sin opción Inquilino si no hay legajo) e inspección obligatoria antes de presupuestar. Migración en 2 fases COMPLETA: fase 1 `story_940_causa_fase1_convivencia` + fase 2 `story_943_causa_fase2_drop` aplicada post-deploy (columnas y type eliminados, prod verificada). Renumeradas de 938/939/940 a 941/942/943 por choque con las stories de Giuliano.
- [x] **Desvío de presupuesto sobre materiales, ponderado por plata** (2026-07-12, STORY-937, commit 6719252) — el "Desvío" del scorecard y "Cumplimiento de presupuesto" de Informes pasan a medir materiales reales (rendición; fallback costo_final − mano de obra para viejas) vs presupuestados, con Σ reales / Σ presupuestados − 1 (una obra chica ya no pesa como una grande). Verificación numérica SQL vs UI: idéntica (8 técnicos, n=71).
- [x] **Terminar la obra con avance obligatorio + gastos que justifican el exceso** (2026-07-11, STORY-936, commit 473a677) — el técnico no puede terminar la ejecución sin al menos una nota de avance, y si rinde materiales por más de lo presupuestado, el exceso debe estar cubierto por gastos imprevistos con ticket (Σ gastos ≥ exceso; bloqueo UI con montos en vivo + rechazo server). Rendir igual o menos no exige nada; los gastos siguen siendo de carga libre en ejecución. Sin migración. Verificada E2E.
- [x] **Validaciones de flujo + archivo de finalizadas** (2026-07-11, STORY-935, commit ddebabd) — (1) no se crea gestión si no hay técnico activo con la especialidad (tablero e inbox); (2) el email del presupuesto al pagador es obligatorio antes de "Aprobar y ejecutar" (marca `presupuesto_enviado_en`, bloqueo UI + server); (3) el resumen de obras ahora saluda "Hola, {propietario}:" (era el único email sin nombre); (4) archivar finalizadas + vista "Archivo" (`/gestiones/archivadas`, nav de staff, RLS respeta ownership del gestor, desarchivable). Verificada E2E.
- [x] **Rendición de materiales + gastos sin aprobación + fee solo lectura** (2026-07-11, STORY-934, commit 7f5ac43) — para terminar la ejecución el técnico rinde total gastado + foto de todos los comprobantes; liquidación = rendido + mano de obra; desvío de materiales visible en Conformidad; fuera la aprobación por gasto y el input de fee en facturación. Verificada E2E.
- [x] **Gastos imprevistos del técnico** (2026-07-11, STORY-932, commit 7c7640f) — el técnico propone gastos en ejecución con foto de ticket obligatoria, el gestor los resuelve, la conformidad se bloquea con gastos pendientes y `costo_final`/nota de cobro los absorben con desglose. Verificada E2E.
- [x] **Deploys automáticos de Giuliano** (2026-07-11, v2 → OBSOLETA 2026-07-12) — el bloqueo de Vercel Hobby a commits de autores no-dueño aplicaba porque el repo era PRIVADO; al hacerse público, la integración Git nativa deploya los commits de cualquier colaborador (verificado: push de Giuliano deployado en 10 segundos con la Action en rojo de fondo). La Action `.github/workflows/deploy-vercel.yml` se ELIMINÓ y el pendiente del token durable de Vercel quedó sin efecto. GiulianoVigetti es colaborador write.

- [x] **Retoques del dashboard de métricas** (2026-07-08, STORY-919, SIN commitear — Fausti revisa en local) — fix bugs (rechazos de asignación por evento; calificación por embed to-one de PostgREST), 5 bloques sectorizados por alcance (caja "En el período" con el filtro en la cabecera; "ahora" arriba, "histórico" al final), embudo→barras, combo de ingresos con toggle de series + tendencia por serie, gradiente de magnitud, tendencia como tasa absoluta (no %), métrica "Tiempo de ciclo" y "Dinero pendiente", tile "Urgentes sin asignar". Detalle completo en `specs/STORY-919.md`.
- [x] **Carga demo para probar métricas** (2026-07-08, STORY-918) — 80 gestiones `[DEMO]` en todas las etapas + gestores/técnicos/cartera demo (`ausitesis+demo…`, pass = usuario123). Sembrado con `scripts/demo-seed.sql`. **Revertir todo:** `./scripts/demo-borrar.sh` (base + fotos), o pedirle a Claude que corra `scripts/demo-borrar.sql` (solo base). IDs y conteos en `scripts/demo-manifest.json`. Se puede mover/editar las gestiones demo sin romper el borrado.
- [x] Correcciones completas de la auditoría 2026-07-06 (STORY-906, commit 950dbf8) — seguridad DB, finanzas, funnel, inbox, realtime, UI, métricas.
