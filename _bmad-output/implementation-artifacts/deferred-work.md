# Trabajo diferido

- **Last-write-wins al editar empleados en paralelo** (detectado en review del fix v1.1 de STORY-103, 2026-07-15, preexistente): si dos admins editan al mismo empleado, el form inline usa inputs no controlados (`defaultValue`) que no se actualizan con el refresh en vivo, y el segundo en guardar pisa en silencio los cambios del primero (`codigo/components/empleados/empleados.client.tsx:91-104`). Riesgo bajo (una inmobiliaria rara vez tiene 2 admins editando a la vez). Si algún día molesta: comparar `updated_at` antes del UPDATE o refrescar `defaultValue` al detectar el evento.

- **Tests unitarios de `codigo/shared/utils/cuil.ts`** (diferido en el fix v1.1 de STORY-923, 2026-07-12): el repo no tiene infraestructura de tests; el placeholder inválido `20301234567` que originó el bug se habría atrapado con un test de `cuilValido`/`errorCuil` (válidos/inválidos/longitud/normalización/ejemplos de placeholders). Si algún día se agrega un runner de tests, este archivo es el primer candidato.
